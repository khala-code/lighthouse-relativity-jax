# Architecture: Fractal Simplicial Lorentzian Engine

## 1. Overview
The Fractal Simplicial Lorentzian Engine is a deterministic, pre-compiled simulation architecture designed to model 4D Lorentzian spacetime without runtime conditional branches (`if-else` statements). The system decouples spatial generation from temporal evolution by compiling a static Euclidean game tree in meta-time and subsequently rotating the resulting trajectory into a deterministic Lorentzian simulation video.

---

## 2. Pre-Runtime Compilation (Levain & Frame Zero)
Before any solver execution begins, the master compilation function (**Levain**) constructs the foundational geometry and locks down **Frame Zero**:
*   **Recursive Simplex Tree:** A 4-simplex partitioned into four real octant cells and a central null centroid, recursively subdivided to scale local resolution organically.
*   **Heegner Number Capping:** Recursion depth is strictly capped at **163 layers** to prevent ultraviolet divergence across the fractal lattice.
*   **Primordial Asymptote Vertices:** Four foundational nodes acting as both mutual repulsors and geodesic termini. These nodes function as asymptotes where information compresses rather than hitting hard singularities.
*   **Zeta Spiral Conduits:** Embedded Euclidean helical templates aligned with the non-trivial zeros of the Riemann zeta function, providing destruction-free information channels.
*   **CMB Void Faces:** The external void faces of the simplex act as a holographic, distorting reflection screen modeled after the Cosmic Microwave Background, seeding micro-scale fluctuations.

---

## 3. The Euclidean Meta-Time Solver
The runtime engine executes entirely within a vectorized, branchless JAX framework, treating physical evolution as a game-theoretic equilibrium search across Euclidean meta-time:
*   **GR vs. Standard Model Minimax Game:** General Relativity (macro-geometry and barycentric weights) and the Standard Model (local standing-wave phase functions at sub-centroids) engage in a minimax optimization.
*   **Discord Minimization:** The system minimizes quantum-geometric friction (discord) and tracks the maximum delta of Euclidean updates ($\Delta_{\text{max}}$) across simplicial faces.
*   **Continuous Alpha-Beta Pruning:** Rather than hard-deleting branches via conditional logic, out-of-bounds sub-trees are smoothly damped through amplitude thresholding and thermodynamic entropy restoration.

---

## 4. Coordinate Transformation & Render Pipeline
Once the solver establishes the optimal "critical line" of moves representing the global Nash equilibrium:
*   **Symplectic Phase Rotation:** The sequence of static Euclidean states undergoes an analytical coordinate transformation (analogous to a generalized Wick rotation) to map timeless geometric configurations into proper metric intervals for Lorentzian spacetime.
*   **Deterministic Rendering:** The resulting barycentric weights and phase distributions are passed through a render pipeline to output a fully deterministic 4D spacetime visualization video, utilizing established Kleinion-mesh simulation techniques.

---

## 5. Core Execution Rules
1.  **Zero Conditionals:** All state transitions, pruning masks, and wave-function updates rely entirely on continuous matrix operations and vectorization.
2.  **Deterministic Pipeline:** No real-time reactivity; computation is front-loaded into Levain's compilation pass, ensuring absolute numerical stability.

## 6. Addendum: Levain Declarative Compilation & Emergent Dynamics

### 1. Levain's Declarative Configuration Engine
Levain acts as the pre-runtime compilation engine, reading high-level declarative configuration files to initialize distinct experimental regimes ("microgames") without manual hardcoding. 
*   **Topological Mapping:** Compiles and tessellates the recursive simplex fractal tree across the non-orientable Klein bottle manifold $\mathcal{M} = S^1 \widetilde{\times} S^1$[cite: 1, 2].
*   **Microgame Targets:** Automatically configures initial parameters for cosmological evolution, binary black hole mergers, or solid-state crystal lattices based on the declarative input config[cite: 1, 2].

### 2. Initial Condition Injection & Gaussian Variance
To break simulation symmetry and avoid artificial stagnation, initial states are seeded dynamically rather than prescribed:
*   **Mass & Defect Placement:** Mass bodies and topological defect clusters are injected into Frame Zero with specified baseline coordinate positions and momentum vectors.
*   **Stochastic Gaussian Variance:** An initial Gaussian variance is applied to position, momentum, and standing-wave node phases. This provides the necessary microscopic phase jitter and thermal seed for natural symmetry breaking.

### 3. Emergent Solver Mechanics
All dynamics—including orbital paths, gravitational attraction, inspiral decays, and wave radiation—emerge directly from the solver's branchless differential field equations rather than preset kinematic functions:
*   **Gradient-Driven Trajectories:** Motion is governed organically by local spatial gradients of the Void Density ($\nabla \Pi_V$) acting as effective gravitational fields[cite: 1, 2].
*   **Energy Dissipation & Ringdown:** Orbital momentum naturally couples into transverse spin-wave modes across the manifold, driving physical inspirals and ringdowns via the Master Extended Bloch Equation and Dirac-Born-Infeld (DBI) saturation radicals[cite: 1, 2].

## 7. Addendum: Modular Geometry & Levain's j-Invariant Configuration

### 1. Levain's Initial Modular Setup
Levain parses declarative configuration files to establish the baseline global conformal shape of the Klein manifold's underlying torus at Frame Zero:
* **Initial Modular Parameter (tau_0):** Levain takes a target initial j-invariant or modular parameter as a configuration input.
* **Global Topology Baseline:** This defines the macroscopic aspect ratios, symmetry properties, and baseline metric configuration before topological defects or mass bodies are injected.

### 2. Runtime Evolution as a Macro-Derivative
While the starting geometry is declaratively configured, its ongoing trajectory is treated strictly as a dynamic runtime derivative:
* **Defect-Driven Perturbation:** Local topological defects, Kleinion charge concentrations, and Void Density backreactions warp the local metric and modular structure.
* **Global Diagnostic Fingerprint:** The runtime engine continuously computes the evolving j-invariant from the system's field distributions and stress-energy backreaction. 

### 3. Macro-Scale Observability
Tracking the j-invariant over meta-time provides an exact, scalar fingerprint of the manifold's global deformation, shifting rapidly during high-energy events such as binary black hole mergers or phase condensation cascades without requiring artificial tuning parameters during runtime execution.

## 8. Addendum: Frame Zero Initialization Protocol & Mexican Hat Asymptotes

### 1. Initialization Protocol: Topological Defect Seeding
To break simulation symmetry without relying on arbitrary or unphysical parameters, Levain initializes the Klein manifold by embedding structured topological defect templates rather than raw matter objects:
* **Defect Centroids:** Concentrated windings (such as Kleinion cores or localized $\mathbb{Z}_2$ parity twists) are introduced at predefined coordinate centroids with initial momentum vectors.
* **Structural Core-Shell Seeds:** Localized phase distortions and initial Void Density gradients ($\Pi_V$) are shaped using deterministic field distributions rather than uniform background noise.

### 2. The "Natural Seed": Mexican Hat Radial Asymptotes
Rather than injecting arbitrary stochastic jitter, Levain applies a **Mexican hat radial profile** (Laplacian-of-Gaussian wavelet) to initialize the field states prior to solver execution:
* **Core-Annulus Balance:** Each defect is initialized with a central core and a matching outer annular counter-weight, providing a natural spatial healing length.
* **Deterministic Symmetry Breaking:** The profile natively forces spontaneous symmetry breaking by creating a balanced gradient slope, allowing non-trivial trajectory evolution without numerical shock.
* **Elimination of Arbitrary Tuning:** Replaces pseudo-random noise generators with a pure geometric invariant, ensuring repeatable, physically anchored simulation paths.

## 9. Addendum: Prime-Locked Branching & Mirror Spiral Interference

### 1. Number-Theoretic Ontology & Temporal Anchoring
Adhering to the foundational axiom that number theory exists independently of Lorentzian spacetime, the simulation architecture derives its structural rules from immutable arithmetic invariants. The pre-runtime Euclidean templating phase (Levain) utilizes prime-locked starting temporal coordinates (T0) mapped precisely to the critical strip of the Riemann zeta function where Re(s) = 0.5.

### 2. Divergent Critical Line Branching & Precession Reversal
At these precise prime T-values, the simulation architecture introduces strategic topological bifurcation points:
* **Bifurcation Junctions:** Prime-locked temporal intervals serve as natural geometric opportunities where the execution engine branches into divergent critical lines.
* **Precession Inversion:** At these junctures, local phase precession vectors are permitted to undergo controlled topological reversal, shifting chiral orientation without introducing illegal conditional branches (if-else statements).

### 3. Mirror Spiral Interference & Nash Equilibrium Determination
The stabilization of these branching trajectories is dictated by wave mechanics across the non-orientable substrate:
* **Mirror Spiral Conduits:** Propagating simulation fields interact dynamically with opposing structural mirror spirals embedded within the recursive fractal simplex tree.
* **Interference States:** The resulting superposition—whether constructive amplification or destructive phase cancellation—reshapes local topological discord.
* **Equilibrium Modulation:** This interference pattern directly modulates the outcome of the minimax optimization game between General Relativity and the Standard Model, dictating the winning equilibrium state and resulting stress-energy backreaction for that specific simulation frame.

## 10. Addendum: Multi-Octant Interference Integration & Cross-Octant Topological Invariants

### 1. The 2D Manifold as an Interference Projection
The 2D Klein manifold $\mathcal{M} = S^1 \widetilde{\times} S^1$ is not treated as an isolated flat simulation surface, but as the holographic interference integration of all 8 Euclidean octants governed by CPT-V symmetry reflections:
* **Hidden Degrees of Freedom:** While the runtime solver computes dynamics directly on the 2D projection, the hidden octants maintain independent phase, winding, and symmetry parameters.
* **Octant Weighting Tensor:** Initial configurations are governed by an 8-component phase weight tensor ($\Theta_{abc}$) that encodes multi-brane reflections across the underlying arithmetic substrate.

### 2. Temporal Indexing via Prime $T$-Axis Skips
Advancing along a prime-locked temporal coordinate ($T_0$) acts as a discrete index through the multi-brane stack:
* **Dynamic State Updates:** Each prime step shifts the phase orientation and boundary conditions of the hidden octants, dynamically altering the starting parameters on the 2D simulation plane.
* **Branchless Multi-Brane Evolution:** Transitions across these temporal intervals update global environmental parameters continuously through smooth mathematical weights, preserving strict branchless execution rules.

### 3. Cross-Octant Invariants as Topological Rivets
Symmetry-breaking invariants that span across two or more octants provide unyielding structural stability to the simulation engine:
* **Macro-Scale Anchors:** Because their winding numbers are protected by the global multi-brane topology rather than local conditions, these multi-octant structures act as topological rivets—functioning physically as heavy core nodes or emergent mass centroids.
* **Interference Modulation:** As the system skips along the prime $T$-axis, mirror spiral phase interference across these multi-octant invariants determines whether the macro-nodes experience constructive amplification (mass/energy accumulation) or destructive phase cancellation at that frame.